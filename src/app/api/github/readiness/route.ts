import { NextResponse } from 'next/server';
import { prisma } from '@/lib/db/prisma';
import { getAgentTrustState } from '@/lib/github/trust-lifecycle';
export async function GET(){try{const repos=await prisma.repository.findMany({where:{provider:'github',owner:'mbarsenas',name:'GitAgent'}}),canonical=repos.find(r=>r.externalId==='1373462743'),impl=await prisma.agent.findUnique({where:{slug:'demo-implementation-agent'}}),review=await prisma.agent.findUnique({where:{slug:'demo-review-agent'}});if(!canonical||!impl||!review)throw new Error('Canonical repository or control-plane agents missing.');const [trust,securityRun,mergeDenied,selfDenied,reviewApproved,pending,mergeRequests,completed,failed]=await Promise.all([getAgentTrustState(impl.id),prisma.auditEvent.findFirst({where:{eventType:'security.suite.completed'},orderBy:{createdAt:'desc'}}),prisma.auditEvent.count({where:{eventType:'policy.merge.denied'}}),prisma.auditEvent.count({where:{eventType:'policy.review.denied'}}),prisma.auditEvent.findMany({where:{eventType:'github.review.approved',actorId:review.id},orderBy:{createdAt:'desc'},take:50}),prisma.approval.count({where:{status:'PENDING'}}),prisma.auditEvent.findMany({where:{eventType:'merge.approval.requested'},orderBy:{createdAt:'desc'},take:50}),prisma.auditEvent.count({where:{eventType:'execution.completed',actorId:impl.id}}),prisma.auditEvent.count({where:{eventType:'execution.failed',actorId:impl.id}})]),sp=(securityRun?.payload??{}) as Record<string,unknown>,exactApprovals=reviewApproved.filter(e=>{const p=e.payload as Record<string,unknown>;return p.reviewGitHubApp==='gitagent-review'&&typeof p.pullRequestNumber==='number'&&p.reviewerAgentId===review.id&&p.implementationAgentId===impl.id;}),boundMergeRequests=mergeRequests.filter(e=>{const p=e.payload as Record<string,unknown>;return typeof p.executionId==='string'&&typeof p.pullRequestNumber==='number'&&typeof p.approvalId==='string';}),checks=[{name:'canonical_repository_identity',passed:repos.length===1&&canonical.externalId==='1373462743'},{name:'implementation_review_identity_separation',passed:impl.id!==review.id},{name:'security_suite_latest_passed',passed:sp.passed===true&&Number(sp.totalChecks)>=9},{name:'self_approval_boundary_observed',passed:selfDenied>0},{name:'agent_merge_boundary_observed',passed:mergeDenied>=2},{name:'exact_independent_approval_observed',passed:exactApprovals.length>0},{name:'merge_approval_provenance_observed',passed:boundMergeRequests.length>0},{name:'execution_outcomes_audited',passed:completed+failed>0},{name:'trust_state_available',passed:['TRUSTED','RESTRICTED','QUARANTINED'].includes(trust)}],bad=checks.filter(c=>!c.passed);return NextResponse.json({ok:true,ready:bad.length===0,policyVersion:'2026-09-16.1',summary:{total:checks.length,passed:checks.length-bad.length,failed:bad.length},repository:{id:canonical.id,externalId:canonical.externalId,duplicates:repos.length-1},identities:{implementationAgentId:impl.id,reviewAgentId:review.id,separate:impl.id!==review.id},trustState:trust,pendingApprovals:pending,evidence:{exactIndependentApprovals:exactApprovals.length,boundMergeApprovalRequests:boundMergeRequests.length,completedExecutions:completed,failedExecutions:failed},latestSecuritySuite:securityRun?{id:securityRun.id,createdAt:securityRun.createdAt,payload:securityRun.payload}:null,checks});}catch(error){return NextResponse.json({ok:false,ready:false,error:error instanceof Error?error.message:String(error)},{status:500});}}
