import { prisma } from '@/lib/db/prisma';
export type HumanDecision='APPROVE'|'REJECT';
export async function decideRestrictedExecution(input:{approvalId?:string;taskId?:string;humanActorId:string;decision:HumanDecision;reason?:string}){const human=await prisma.user.findUnique({where:{id:input.humanActorId}});if(!human)throw new Error('Human actor not found.');const approval=input.approvalId?await prisma.approval.findUnique({where:{id:input.approvalId},include:{task:true}}):await prisma.approval.findFirst({where:{taskId:input.taskId,action:'restricted.execute',status:'PENDING'},include:{task:true},orderBy:{requestedAt:'desc'}});if(!approval)throw new Error('Restricted execution approval not found.');if(approval.action!=='restricted.execute')throw new Error('Approval is not for restricted execution.');if(input.taskId&&approval.taskId!==input.taskId)throw new Error('approvalId does not belong to taskId.');if(approval.status!=='PENDING')return{taskId:approval.taskId,approvalId:approval.id,decision:approval.status,idempotentReplay:true,taskStatus:approval.task.status,approvedByUserId:approval.actorId};const approved=input.decision==='APPROVE',now=new Date();const [updated,task]=await prisma.$transaction([prisma.approval.update({where:{id:approval.id},data:{status:approved?'APPROVED':'REJECTED',actorId:human.id,decidedAt:now,reason:input.reason??null}}),prisma.task.update({where:{id:approval.taskId},data:{status:approved?'QUEUED':'CANCELLED'}}),prisma.auditEvent.create({data:{taskId:approval.taskId,eventType:approved?'restricted.execution.approved':'restricted.execution.rejected',actorType:'human',actorId:human.id,payload:{approvalId:approval.id,decision:input.decision,reason:input.reason??null,policyVersion:'2026-09-16.1'}}})]);return{taskId:approval.taskId,approvalId:updated.id,decision:approved?'APPROVED':'REJECTED',idempotentReplay:false,taskStatus:task.status,approvedByUserId:human.id};}
