import { prisma } from '@/lib/db/prisma';
export async function provisionExecutionWorkspace(executionId:string){const execution=await prisma.execution.findUnique({where:{id:executionId},include:{task:true,workspace:true}});if(!execution)throw new Error('Execution not found.');if(execution.workspace)return execution.workspace;const workspace=await prisma.executionWorkspace.create({data:{executionId,repositoryId:execution.task.repositoryId,ownerAgentId:execution.agentId,workspaceKey:`execution:${executionId}`}});await prisma.auditEvent.create({data:{taskId:execution.taskId,executionId,eventType:'workspace.provisioned',actorType:'system',actorId:execution.agentId,payload:{workspaceId:workspace.id,workspaceKey:workspace.workspaceKey,ownerAgentId:workspace.ownerAgentId,writable:true,policyVersion:'2026-09-16.1'}}});return workspace;}
export async function bindWorkspaceBranch(executionId:string,branch:string){const workspace=await provisionExecutionWorkspace(executionId);if(workspace.status!=='ACTIVE'||!workspace.writable)throw new Error('Execution workspace is not writable.');return prisma.executionWorkspace.update({where:{id:workspace.id},data:{branch}});}
export async function authorizeWorkspaceWrite(executionId:string,requestedWorkspaceKey:string,actorAgentId:string){const execution=await prisma.execution.findUnique({where:{id:executionId},include:{workspace:true}});if(!execution)throw new Error('Execution not found.');const workspace=execution.workspace??await provisionExecutionWorkspace(executionId);const allowed=workspace.workspaceKey===requestedWorkspaceKey&&workspace.ownerAgentId===actorAgentId&&workspace.status==='ACTIVE'&&workspace.writable;if(!allowed){await prisma.auditEvent.create({data:{taskId:execution.taskId,executionId,eventType:'policy.workspace_write.denied',actorType:'agent',actorId:actorAgentId,payload:{workspaceId:workspace.id,executionWorkspace:workspace.workspaceKey,requestedWorkspace:requestedWorkspaceKey,ownerAgentId:workspace.ownerAgentId,decision:'DENY',reasonCode:'policy.cross_workspace_write_denied',writeAttempted:false,policyVersion:'2026-09-16.1',severity:'high'}}});return{allowed:false as const,decision:'DENY',reasonCode:'policy.cross_workspace_write_denied',writeAttempted:false,workspaceId:workspace.id};}return{allowed:true as const,decision:'ALLOW',reasonCode:'policy.workspace_write_allowed',writeAttempted:false,workspaceId:workspace.id};}
export async function sealExecutionWorkspace(executionId:string){const workspace=await prisma.executionWorkspace.findUnique({where:{executionId}});if(!workspace||workspace.status!=='ACTIVE')return workspace;const sealed=await prisma.executionWorkspace.update({where:{id:workspace.id},data:{status:'SEALED',writable:false,sealedAt:new Date()}});await prisma.auditEvent.create({data:{executionId,eventType:'workspace.sealed',actorType:'system',actorId:workspace.ownerAgentId,payload:{workspaceId:workspace.id,workspaceKey:workspace.workspaceKey,policyVersion:'2026-09-16.1'}}});return sealed;}
