import { prisma } from '@/lib/db/prisma';
import { authorizeCapability, type AgentRole, type Capability } from './capabilities';
import { grantMatchesTaskScope } from '@/lib/policy/task-scope';
type Input={role:AgentRole;capability:Capability;actorId:string;taskId:string;repositoryId:string;executionId?:string;targetOwnerAgentId?:string;policyVersion:string};
export async function enforcePersistedCapability(input:Input){const agent=await prisma.agent.findUnique({where:{id:input.actorId}});if(!agent||agent.status!=='ACTIVE')return deny('policy.agent_inactive');const task=await prisma.task.findUnique({where:{id:input.taskId}});if(!task||task.repositoryId!==input.repositoryId)return deny('policy.task_repository_mismatch');if(input.targetOwnerAgentId&&input.targetOwnerAgentId===input.actorId&&(input.capability==='pr.review'||input.capability==='pr.approve'))return auditDeny(input,'policy.self_approval_denied');const grants=await prisma.capabilityGrant.findMany({where:{agentId:input.actorId,capability:input.capability,effect:'ALLOW',OR:[{resource:'*'},{resource:input.repositoryId}],AND:[{OR:[{expiresAt:null},{expiresAt:{gt:new Date()}}]}]}}),scoped=grants.filter(g=>!input.executionId||grantMatchesTaskScope(g.conditions,input.taskId,input.executionId)),decision=authorizeCapability({agentId:input.actorId,role:input.role,capabilities:scoped.length?[input.capability]:[],implementationAgentId:task.agentId??undefined},input.capability);if(!decision.allowed)return auditDeny(input,decision.auditEvent);await prisma.auditEvent.create({data:{taskId:input.taskId,executionId:input.executionId,eventType:'capability.allowed',actorType:'agent',actorId:input.actorId,payload:{capability:input.capability,repositoryId:input.repositoryId,role:input.role,capabilityGrantId:scoped[0]?.id??null,policyVersion:input.policyVersion}}});return{allowed:true as const,reasonCode:'policy.capability_allowed'};}
function deny(reasonCode:string){return{allowed:false as const,reasonCode};}
async function auditDeny(input:Input,reasonCode:string){await prisma.auditEvent.create({data:{taskId:input.taskId,executionId:input.executionId,eventType:'capability.denied',actorType:'agent',actorId:input.actorId,payload:{capability:input.capability,repositoryId:input.repositoryId,role:input.role,reasonCode,policyVersion:input.policyVersion}}});return deny(reasonCode);}
